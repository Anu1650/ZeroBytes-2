{
  "name": "mernauth",
  "version": "1.0.0",
  "description": "Root package to easily start MernAuth components",
  "scripts": {
    "start": "npm start --prefix server",
    "server": "npm run server --prefix server",
    "dev": "npm run dev --prefix server",
    "install-all": "npm install --prefix server"
  }
}
